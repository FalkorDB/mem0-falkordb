version: '3.8'

services:
  falkordb:
    image: falkordb/falkordb:edge
    container_name: mem0-falkordb-demo
    ports:
      - "6379:6379"
    volumes:
      - falkordb_data:/data
    restart: unless-stopped

volumes:
  falkordb_data:
